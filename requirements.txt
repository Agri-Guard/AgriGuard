# ─────────────────────────────────────────────────────────────────────────────
# AgriGuard Python dependencies
# Install: pip install -r requirements.txt
# Python >= 3.10 required
# ─────────────────────────────────────────────────────────────────────────────

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.111.0,<1.0
uvicorn[standard]>=0.29.0,<1.0
python-multipart>=0.0.9        # for form/file uploads

# ── Data validation ───────────────────────────────────────────────────────────
pydantic>=2.0.0,<3.0
pydantic-settings>=2.0.0

# ── ML / data science ─────────────────────────────────────────────────────────
numpy>=1.26.0,<2.0
pandas>=2.2.0,<3.0
scikit-learn>=1.4.0,<2.0
xgboost>=2.0.0                 # price forecast model; falls back to sklearn if absent

# ── HTTP client (data download + frontend → backend calls) ───────────────────
requests>=2.31.0,<3.0

# ── Frontend ──────────────────────────────────────────────────────────────────
streamlit>=1.35.0
plotly>=5.22.0

# ── Database (optional — SQLite works out of the box) ─────────────────────────
sqlalchemy>=2.0.0,<3.0
pymysql>=1.1.0                 # only needed for MySQL; comment out for SQLite-only
python-dotenv>=1.0.0

# ── Image analysis (optional — enables Anthropic vision endpoint) ─────────────
anthropic>=0.28.0              # only used if ANTHROPIC_API_KEY is set
Pillow>=10.3.0

# ── Dev / testing ─────────────────────────────────────────────────────────────
pytest>=8.0.0
httpx>=0.27.0                  # async test client for FastAPI