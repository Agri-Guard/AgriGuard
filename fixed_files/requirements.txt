# AgriGuard — verified working requirements
# Tested: fresh venv install + `backend.app.main` boots + `pytest tests/` runs
# (10/18 pass — the 8 failures are pre-existing contract issues, see README
# "Known issues", not missing/broken dependencies).

# ── API ──────────────────────────────────────────────────────────────────
fastapi==0.136.1
uvicorn==0.46.0
python-multipart==0.0.28
python-dotenv==1.2.2
pydantic==2.13.4
pydantic-settings==2.14.1

# ── ML & Data ────────────────────────────────────────────────────────────
pandas==2.3.3
numpy==2.4.4
scikit-learn==1.8.0
prophet==1.3.0
xgboost==3.2.0
joblib==1.5.3

# ── Dashboard (frontend/) ────────────────────────────────────────────────
streamlit==1.57.0
plotly==6.7.0
matplotlib==3.10.9

# ── HTTP ─────────────────────────────────────────────────────────────────
httpx==0.28.1
requests==2.34.0

# ── Testing (tests/test_api.py needs these; not required to run the app) ─
pytest==8.3.4

# ── Removed vs. the previous requirements.txt (unused — nothing in the
#    codebase imports these; add back if/when you actually build the
#    features they're for) ────────────────────────────────────────────────
#   sqlalchemy, alembic, aiomysql, PyMySQL  — no DB layer is wired in yet
#                                              (routers/prices.py needs this,
#                                              see README Known issues)
#   python-jose, bcrypt, passlib            — no auth/JWT code exists yet
#   lightgbm, scipy, seaborn                — not imported anywhere
